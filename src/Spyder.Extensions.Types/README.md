# Spyder.Extensions.Types
Collection of extension methods for types

- Arrays
- Strings

## Usage
    string message = "Hello World";
    
    if(message.IsNullOrEmpty)
    {
        Console.WriteLine("There is nothing to say");
    {